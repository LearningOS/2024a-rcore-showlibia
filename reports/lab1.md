1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 **以下各位** 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

   > 

2. 此外，我也参考了 **以下资料** ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

   > 

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

## 简答作业

本章实现了系统调用`sys_task_info`, 用于获取任务状态,任务使用的系统调用及调用次数,系统调用时刻距离任务第一次呗调度时刻的时长

1. >  RustSBI version 0.3.0-alpha.2, adapting to RISC-V SBI v1.0.0

在ci-user目录下make test CHAPTER=2,运行ch2b_bad_*.rs程序.运行结果如图所示,其中产生了缺页错误和非法指令.也就是在目前的内核中,出现了非法地址访问和U特权级越级使用S特权级指令的错误．这两个错误均被kernel检测到并杀死进程.

![image-20241031005523396](https://raw.githubusercontent.com/showlibia/img/main/image/1730307323_0.png)

2. 1. a0中存的是`push_context` 的返回值，这个值是内核栈压入 Trap 上下文之后的栈顶，可以保证在 `__restore` 函数中 `sp` 仍然可以指向内核栈的栈顶

   两种应用场景： `trap_handler` 返回之后，从栈上的 Trap 上下文恢复寄存器；`run_next_app`切换运行下一个应用程序时,从S态切换到U态，恢复寄存器到达启动应用程序所需要的上下文状态。

   2. 特殊处理了sstatus、sepc、sscratch，先恢复CSR，才能正确恢复三个使用的通用寄存器.
   
   > `sstatus`: 确保切换到U特权等级
   >
   > `sepc`: 切换到U态下的程序入口地址,确保原程序继续正确执行
   >
   > `sscratch`: 指向内核栈

3. x4是thread register,在目前单线程OS中用不到, x2用来存放stack pointer的值,指向kernel stack
4. sp 中存储的是用户栈栈顶的 sp， sscratch 中存储的是内核栈栈顶的 sp。
5. 状态切换发生在 sret 指令, sret将sstatus的SPP设置为U mode,同时将 `pc` 的值设置为 `sepc`
6. 和4中相反
7. ecall

